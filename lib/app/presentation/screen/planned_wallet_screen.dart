import 'package:flutter/cupertino.dart';

class PlannedWalletScreen extends StatelessWidget {
  const PlannedWalletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("PlannedWallet"));

  }
}